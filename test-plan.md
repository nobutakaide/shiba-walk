# 拒否柴解放ゲーム テスト計画書 (test-plan.md)

対象仕様書: `spec.md` (v0.1 draft)
対象実装: `index.html`(実装後)
作成者ペルソナ: test-planner

本ドキュメントは、spec.mdに定義された仕様に対して、実装完了後にQAが上から順に実行すれば受け入れ基準(11章)を網羅的に検証できるテストケース一覧である。実装前の現時点では「これから実装される機能」に対する計画であり、テストIDごとに合否欄を追記して使う想定。

---

## 0. テスト実施上の前提・注意事項

- **通常系(正常系)テスト**: 実機のマウス/ペン/トラックパッドでのドラッグ操作で実施する。座標はブラウザの開発者ツール(要素のスタイル`left/top`、またはDOM上のCSS変換値)で犬・パン屋アイコンの現在位置を確認しながら、そこからの相対位置としてドラッグの始点・終点を決める。
- **境界値テスト**: 人間の手操作では px単位・ms単位の精度を再現するのが難しいため、可能であれば開発者ツールのコンソールから `#gestureCanvas` に対して `pointerdown`/`pointermove`/`pointerup` の `PointerEvent` を合成し、座標・タイミングを厳密に制御して発火させる方法を推奨する(例: `el.dispatchEvent(new PointerEvent("pointermove", {clientX, clientY, bubbles:true}))` を一定間隔で発行するスクリプトを用意する)。手動操作で行う場合は「ほぼ閾値付近」を狙い、コード上のしきい値定数(5章・10章)と実測ログ(実装側でデバッグログを仕込める場合はコンソール出力を確認)を突き合わせて判定する。
- **`lastHeading`(直前の移動方向)に依存するテスト**(RETRACE等)は、拒否柴発生前に矢印キーでどの方向に犬を歩かせていたかに依存する。各テストケースの手順内で「発生直前は◯◯キーのみを1.5秒以上押して直線移動させる」ことを明記し、方向を固定できるようにする。
- **`bakeryPos`(パン屋アイコン位置)** は固定表示だが具体的な座標は実装依存のため、実施時に画面上の実際の表示位置を確認し、そこを終点座標として使う。
- 各テストケースの「期待結果」に対する実測は、目視(アニメーション・表示の変化)、および可能であればブラウザの状態変数をコンソールから確認(`window`にデバッグ用に状態を露出している場合)して判定する。
- テストは新規ページロード(またはリロード)から開始し、他のテストの副作用(スコア累積等)を持ち込まない状態で実施することを基本とする。特記がない限り「犬種選択→任意の犬種選択→WALKING開始」から手順を始める。

---

## 1. 状態遷移のテスト

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| ST-01 | BREED_SELECT → WALKING | `index.html`を開く。犬種選択画面で「茶柴」をクリックする。 | 選択画面が閉じ、ゲーム画面(`#gameArea`)が表示される。選んだ犬(茶柴の配色)が表示され、矢印キーで移動できる = `WALKING`状態に入っている。 |
| ST-02 | WALKING → REFUSING(自動遷移) | WALKINGに入った状態で矢印キーを押して犬を動かし続け、拒否柴発生タイマー満了を待つ。 | ある時点で犬が自動的に停止・座り込みポーズに変化し、`.refusing`クラスが付与され、`#refusalBanner`が表示される。この時点で`REFUSING`状態に入る。 |
| ST-03 | REFUSING中は矢印キーが無効 | REFUSING中に矢印キー(上下左右)を押す。 | 犬の位置が一切変化しない(座り込んだまま)。マーキングも生成されない。 |
| ST-04 | REFUSING → WALKING(ジェスチャー成功) | REFUSING中に、8種のいずれか(例: TREATの小さい丸)を正しく描く。 | `.refusing`クラスが除去され、通常ポーズへ0.2秒程度でアニメーション復帰。成功エフェクト(星/ハート等)と`+xx`スコアポップアップが表示され、`WALKING`に戻り矢印キーで再度移動できる。 |
| ST-05 | WALKING → REFUSING → WALKING の繰り返し | ST-02〜ST-04を連続して2〜3回繰り返す。 | 毎回同様に座り込み→ジェスチャー解除→歩行再開のサイクルが問題なく繰り返される。`#episodeCounter`のカウントが解除ごとに1ずつ増える(2章参照)。 |
| ST-06 | WALKING/REFUSING → RESULT(規定回数到達) | 8章の終了条件テスト(END-01)を参照。8回分の拒否柴を解決する。 | 8回目の解除が完了した瞬間に`RESULT`画面へ遷移する。 |
| ST-07 | RESULT → BREED_SELECT(再プレイ) | RESULT画面で「もう一度あそぶ」ボタンをクリックする。 | `BREED_SELECT`(犬種選択画面)に戻る。スコア表示が0、エピソードカウンタが0、画面上の花・マーキングが全てクリアされている(6.3節)。 |
| ST-08 | RESULT後の再プレイでゲームが正常に再始動する | ST-07の後、再度犬種を選択する。 | 新しいラウンドとしてWALKINGから正常に開始し、拒否柴タイマー等が初回条件(TR-01)からやり直しになる。 |

---

## 2. 拒否柴発生条件のテスト

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| TR-01 | 初回発生条件(時間+移動距離の両方必要) | WALKING開始直後、矢印キーを一切押さずに1.2秒以上待機する。 | 移動していない(累計移動距離0px)ため、初回拒否柴は発生しない(30px移動していないため)。 |
| TR-02 | 初回発生条件: 移動距離のみ満たし時間未満 | WALKING開始直後、素早く連打的に矢印キーを押して30px以上動かすが、開始から1,200ms未満のタイミングで観察する。 | 1,200ms経過前は拒否柴が発生しない。 |
| TR-03 | 初回発生条件: 両方満たした直後 | WALKING開始後、矢印キーで継続的に犬を動かし、累計移動距離が30px以上、かつゲーム開始から1,200ms以上経過するようにする。その時点以降を観察する。 | 両条件成立後、初回の拒否柴が発生する(即座でなくてもよいが、それ以前には発生しない)。 |
| TR-04 | 初回発生の境界値(移動距離29px) | 矢印キー入力を細かく制御し、累計移動距離をおよそ29px程度に留めた状態で1,200ms以上待機する(境界値操作はコンソールでの座標ログ確認を推奨)。 | 30px未満のため拒否柴が発生しない。 |
| TR-05 | 初回発生の境界値(移動距離30px超過直後) | 累計移動距離が30pxを僅かに超えた瞬間の挙動を確認する。 | 30px到達(超過)した後、1,200ms条件も満たしていればその時点以降で拒否柴が発生し得る。 |
| TR-06 | 2回目以降の発生間隔(下限4,000ms) | 1回目の拒否柴を解除した直後からタイマーを計測する(手動でストップウォッチ、またはconsole.timeなどを使用)。 | 解除から次の拒否柴発生までの間隔が4,000ms未満になることはない(複数回試行して最小値を観測し、4,000ms以上であることを確認)。 |
| TR-07 | 2回目以降の発生間隔(上限9,000ms) | 解除後、9,000msを超えても拒否柴が発生しない場合は異常。複数回(5回以上)試行して間隔を記録する。 | 全ての試行で間隔が9,000ms以内に収まっている。 |
| TR-08 | 2回目以降は移動距離条件が不要であることの確認 | 拒否柴を1回解除した直後、矢印キーを押さずにその場で待機する(移動距離0のまま)。 | 移動していなくても、4〜9秒のランダム時間経過後に次の拒否柴が発生する(初回のみ移動条件が必要という仕様の確認)。 |
| TR-09 | 発生間隔のランダム性 | 2回目以降の発生を10回程度記録し、間隔の分布を確認する。 | 間隔が常に同じ値(固定値)ではなく、4,000〜9,000msの範囲内でばらついている。 |

---

## 3. 8種ジェスチャーの正常系・境界値・識別性テスト

各ジェスチャーの座標は、実施時点の犬の中心座標(`dogCenter`)・パン屋アイコン座標(`bakeryPos`)を実機で確認したうえで、そこからの相対距離・相対角度として読み替えて実施すること。以下「犬の近く」は`DOG_PROXIMITY_RADIUS = 70px`以内を指す。

### 3.1 TUG(ぐいぐい引っ張る)

条件: `startDistToDog<=70`, `duration<=1200ms`, `reversalsPrimary>=4`, `avgSpeed>=500px/s`, 主軸bbox幅15〜90px, 副軸bbox幅<40px

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-TUG-01(正常系) | REFUSING中、犬の中心付近(70px以内)でpointerdownし、水平方向に振幅約25px前後で左右に素早く5〜6往復(reversalsPrimaryが4以上になるように)ジグザグにドラッグし、約300〜500ms以内でpointerupする。垂直方向のブレは40px未満に抑える。 | TUGとして認識され、拒否柴が解除される(短時間なので高スコアになるはず、6章参照)。 |
| G-TUG-02(境界値: reversals=3) | 同様の操作だが往復回数を減らし、方向反転が3回だけになるようにする。 | reversalsPrimary<4のためTUGとして成立しない(未認識、または他条件に該当しなければ「ちがうよ」リアクション)。 |
| G-TUG-03(境界値: duration超過) | 振幅・往復回数は正常系と同じだが、ゆっくり動かしてduration=1300ms程度にする。 | duration>1200msのためTUG不成立。 |
| G-TUG-04(境界値: 主軸bbox幅超過) | 振幅を大きく(往復の端から端が100px程度)して同様に往復させる。 | 主軸bbox幅が90pxを超えるためTUG不成立(SONGなど別ジェスチャーとして誤認識されないかも合わせて確認)。 |
| G-TUG-05(境界値: 副軸ブレ超過) | 水平往復させつつ、縦方向にも45px程度ブレさせる。 | 副軸bbox幅が40px以上となりTUG不成立。 |
| G-TUG-06(境界値: 開始位置が範囲外) | 犬から80px離れた位置でpointerdownし、TUGと同様の高速往復ジェスチャーを行う。 | `startDistToDog`が70pxを超えるためTUG不成立。 |
| G-TUG-07(識別性) | TUGの操作を、より遅い速度(avgSpeed<500px/s)で行う。 | 速度条件を満たさずTUG不成立。SONG(波線)の条件(waveCount>=3, duration400〜3000ms)を偶発的に満たしていないか確認し、意図せず別ジェスチャーとして成立していないことを確認する。 |

### 3.2 MULTI_PULL(色々な方向へ引っ張る)

条件: 直近3秒以内に `straightness>=0.7`, `pathLength>=40px`, `startDistToDog<=70` を満たすストロークが3本以上、かつ45°刻み8方位のうち異なる方位が3つ以上。

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-MULTI-01(正常系) | 犬の近くから、(1)右方向へ約60pxまっすぐドラッグ→pointerup、(2)続けて犬の近くから下方向へ約60pxまっすぐドラッグ→pointerup、(3)続けて犬の近くから左上方向へ約60pxまっすぐドラッグ→pointerup、を合計3秒以内に行う。各ストロークは直線的(straightness>=0.7目安)。 | 3本目のストローク確定時点でMULTI_PULLとして認識され、拒否柴が解除される。 |
| G-MULTI-02(境界値: 方位が2種類のみ) | 右方向・右方向(ほぼ同じ角度)・下方向、のように45°刻みで2方位にしか分類されない3本を3秒以内に行う。 | 異なる方位が3つ未満のためMULTI_PULL不成立。各ストロークが個別にTAXI以下の判定にかけられ、他ジェスチャーとして誤認識していないか確認する。 |
| G-MULTI-03(境界値: 3秒超過) | 1本目を描いた後、3.2秒程度待ってから2本目・3本目を1秒以内に描く(3方位を満たす向きで)。 | 1本目がローリングバッファから外れており、直近3秒以内の対象ストロークが2本以下となるためMULTI_PULL不成立。 |
| G-MULTI-04(境界値: 距離不足) | 3方位を満たす向きで、各ストロークの長さを35px程度(40px未満)にする。 | pathLength不足によりMULTI_PULL不成立。 |
| G-MULTI-05(境界値: 開始位置が範囲外) | 3方位を満たす向きだが、各ストロークの始点を犬から80px離れた位置にする。 | startDistToDog超過によりMULTI_PULL不成立。 |
| G-MULTI-06(識別性・優先順位確認) | 1本目・2本目のストロークを、それぞれ単体でTAXI/TREAT/HUG等の条件を満たさない形(短い直線)で描き、3本目で3方位条件を完成させる。 | 1本目・2本目確定時点ではMULTI_PULLの条件(3本未満)を満たさず、かつ他ジェスチャーとしても未認識のまま拒否柴が継続する。3本目確定時に初めてMULTI_PULLとして解除される。仕様上、MULTI_PULLは他の判定より最優先(①)であることを併せて確認する。 |

### 3.3 RETRACE(来た道を戻る)

条件: `straightness>=0.75`, `startDistToDog<=70`, `pathLength>=60px`, 方向角が`lastHeading+180°`の±30°以内

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-RETRACE-01(正常系) | 拒否柴発生直前に矢印キー「→(右)」のみを1.5秒以上押し続けて犬を右方向に歩かせる(`lastHeading≈0°`=東向き)。REFUSING突入後、犬の近くから左方向(≈180°)へまっすぐ70px程度ドラッグする。 | RETRACEとして認識され解除される。 |
| G-RETRACE-02(境界値: 角度31°ずれ) | 上記同様だがドラッグ方向を左から31°傾ける(lastHeading+180°から±30°の範囲外)。 | 角度条件を満たさずRETRACE不成立。 |
| G-RETRACE-03(境界値: 角度29°ずれ) | ドラッグ方向を左から29°傾ける(範囲内ギリギリ)。 | RETRACEとして成立する。 |
| G-RETRACE-04(境界値: pathLength=59px) | 正しい逆方向へ約59pxのみドラッグする。 | pathLength不足によりRETRACE不成立。 |
| G-RETRACE-05(境界値: pathLength=61px) | 正しい逆方向へ約61pxドラッグする。 | RETRACEとして成立する。 |
| G-RETRACE-06(境界値: straightness不足) | 逆方向・長さは条件を満たすが、途中でカーブさせて直線性を落とす(straightness<0.75になるよう蛇行させる)。 | straightness不足によりRETRACE不成立。 |
| G-RETRACE-07(識別性) | lastHeadingが東向きの状態で、逆方向ではなく直交方向(90°)へ同様の直線ドラッグを行う。 | 角度条件を満たさずRETRACE不成立。他ジェスチャー(LEAD_TO_BAKERYなど)に誤認識していないか確認する。 |

### 3.4 LEAD_TO_BAKERY(パン屋に連れていく)

条件: `startDistToDog<=70`, 終点が`bakeryPos`から40px以内, `straightness>=0.6`, 方向角が`angle(dogCenter→bakeryPos)`の±25°以内, `pathLength>=distance(dogCenter,bakeryPos)*0.6`

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-BAKERY-01(正常系) | 画面上の`#bakeryIcon`表示位置を確認する。犬の近くからパン屋アイコンへ向けてほぼ直線にドラッグし、アイコンの中心から40px以内で指を離す。ドラッグ距離は犬〜パン屋間の距離の60%以上になるようにする。 | LEAD_TO_BAKERYとして認識され解除される。 |
| G-BAKERY-02(境界値: 終点が41px) | 同様のドラッグだが、パン屋アイコンの手前41px程度で止める。 | 終点距離条件を満たさず不成立。 |
| G-BAKERY-03(境界値: 終点が39px) | パン屋アイコンから39px程度の位置で止める。 | 成立する。 |
| G-BAKERY-04(境界値: pathLength不足) | 直線方向は合っているが、始点をパン屋寄りにして実ドラッグ距離を犬〜パン屋間距離の59%程度に抑える(終点条件は満たす)。 | pathLength不足で不成立。 |
| G-BAKERY-05(境界値: 角度26°ずれ) | 犬からパン屋への正しい向きから26°ずらした方向へドラッグし、終点だけ帳尻を合わせて40px以内に収める(ドラッグを途中で軽く曲げる)。 | 角度条件不成立(直線ではなく曲がっているためstraightnessまたは角度条件のどちらかで弾かれることを確認)。 |
| G-BAKERY-06(境界値: straightness不足) | 終点・方向は合わせつつ大きく迂回する軌跡でドラッグする(straightness<0.6)。 | straightness不足で不成立。 |
| G-BAKERY-07(識別性) | パン屋と反対方向にドラッグしてから引き返してパン屋方向へ向かう(結果的に終点はパン屋付近だがstraightnessが低い)。 | straightness不足等により不成立となることを確認し、単純な「終点がパン屋に近ければ何でも成立する」誤動作がないことを確認する。 |

### 3.5 TREAT(おやつをあげる・小さい丸)

条件: `closedRatio<=0.35`, `avgRadius`8〜30px, `radiusCV<=0.35`, `distance(centroid,dogCenter)<=70`

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-TREAT-01(正常系) | 犬の近くで、半径15px程度の小さな円を1周描き、始点付近で指を離す(始点と終点がほぼ一致)。 | TREATとして認識され解除される。 |
| G-TREAT-02(境界値: avgRadius=7px) | さらに小さい円(半径7px程度)を描く。 | avgRadius不足で不成立(MIN_POINTS/描画のしやすさにも留意)。 |
| G-TREAT-03(境界値: avgRadius=8px) | 半径8px程度の円を描く。 | 成立する。 |
| G-TREAT-04(境界値: avgRadius=30px) | 半径30px程度の円を描く。 | 成立する。 |
| G-TREAT-05(境界値: avgRadius=31px、HUGとの隙間確認) | 半径31px程度の円を描く(HUGの最小35pxにも届かない中間サイズ)。 | TREAT・HUGいずれの範囲にも該当せず不成立(空白帯の確認)。 |
| G-TREAT-06(境界値: closedRatio超過) | 円を描くが最後を閉じずに、始点から離れた位置(円の直径の半分以上)で指を離す。 | closedRatio超過で不成立。 |
| G-TREAT-07(境界値: radiusCV超過) | いびつな形(一部が尖った・潰れた円)を描き、半径のばらつきを大きくする。 | radiusCV超過で不成立(TAXIとして誤認識されないかも確認、cornerCount次第)。 |
| G-TREAT-08(境界値: 犬から遠い) | 犬から80px離れた場所で同条件の円を描く。 | distance(centroid,dogCenter)超過で不成立。 |

### 3.6 SONG(歌をうたう・波線)

条件: `closedRatio>0.5`, `waveCount>=3`, `duration`400〜3000ms, `startDistToDog<=90`

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-SONG-01(正常系) | 犬の近く(90px以内)から、横方向に音符/波のような線を描く。上下に振幅8px以上の山と谷を4〜5個作りながら横に移動し、始点と終点が離れた状態(閉じない)で、描画に1秒前後かける。 | SONGとして認識され解除される。 |
| G-SONG-02(境界値: waveCount=2) | 波の山谷を2個だけにする。 | waveCount不足で不成立。 |
| G-SONG-03(境界値: waveCount=3ちょうど) | 波の山谷をちょうど3個にする。 | 成立する。 |
| G-SONG-04(境界値: duration=399ms) | 同じ波形をできるだけ素早く(0.4秒未満)描く。 | duration不足で不成立(TUGとして誤認識していないか、reversalsPrimary/主軸幅の条件も併せて確認)。 |
| G-SONG-05(境界値: duration=3001ms) | 同じ波形をゆっくり(3秒超)描く。 | duration超過で不成立。 |
| G-SONG-06(境界値: closedRatio<=0.5) | 波線の終点を意図的に始点近くまで戻す(閉じ気味にする)。 | closedRatio条件を満たさず(0.5以下になり)不成立。 |
| G-SONG-07(境界値: 開始位置が91px) | 犬から91px離れた位置から同様の波線を描く。 | startDistToDog超過で不成立。 |
| G-SONG-08(識別性) | 振幅を8px未満の細かい波(ジッター程度)にする。 | waveCountの有効条件(移動量8px以上)を満たさずカウントされず、waveCount不足で不成立。 |

### 3.7 HUG(抱っこする・犬を囲む大きい丸)

条件: `enclosesDog===true`(closedRatio<=0.35 かつ 犬のbbox4隅がcentroidからavgRadius*0.6〜1.4の範囲内), `avgRadius`35〜100px, `radiusCV<=0.35`, `distance(centroid,dogCenter)<=25`

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-HUG-01(正常系) | 犬の体全体を大きく囲むように、犬を中心とした半径50px程度の円を1周描き、始点付近で閉じる。 | HUGとして認識され解除される。 |
| G-HUG-02(境界値: avgRadius=34px) | 犬をぎりぎり囲めるかどうかの半径34px程度の円を描く。 | avgRadius不足で不成立(犬を完全に囲めない可能性が高いこととも整合)。 |
| G-HUG-03(境界値: avgRadius=100px超) | 半径110px程度の非常に大きな円を描く。 | avgRadius超過で不成立。 |
| G-HUG-04(境界値: 中心オフセット26px) | 円の中心を犬の中心から26px程度ずらした位置に描く(円としては犬を囲んでいるつもり)。 | distance(centroid,dogCenter)超過で不成立(enclosesDogの4隅条件も崩れる可能性が高い)。 |
| G-HUG-05(境界値: 閉じていない) | 円を描くが最後を閉じずに(始点から離れた位置で)指を離す。 | closedRatio超過によりenclosesDog不成立、HUG不成立。 |
| G-HUG-06(識別性) | 犬を囲む円ではなく、犬を囲む四角形(角がはっきりした四角)を描く。 | 5.2の優先順位ではTAXI(2)がHUG(4)より先に判定されるため、cornerCount>=3等TAXIの条件を満たせばTAXIとして認識されることを確認する(HUGではなくTAXIが成立する想定)。 |

### 3.8 TAXI(タクシーを呼ぶ・四角形)

条件: `closedRatio<=0.35`, `cornerCount>=3`, `bboxW/bboxH`0.4〜2.5, `radiusCV>0.35`, `distance(centroid,dogCenter)<=100`

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-TAXI-01(正常系) | 犬の近くで、右へ→下へ→左へ→上へ(始点付近に戻る)、四角形を描くように1ストロークでドラッグする。 | TAXIとして認識され解除される。 |
| G-TAXI-02(境界値: cornerCount=2) | 角を2つだけ持つL字型の軌跡を描く(閉じる)。 | cornerCount不足で不成立。 |
| G-TAXI-03(境界値: cornerCount=3ちょうど) | 三角形を描く(始点付近に戻る)。 | 成立する。 |
| G-TAXI-04(境界値: 縦横比0.39) | 非常に縦長(幅高さ比0.39程度)の四角形を描く。 | bboxW/bboxH範囲外で不成立。 |
| G-TAXI-05(境界値: 縦横比0.41) | 幅高さ比0.41程度の縦長四角形を描く。 | 成立する。 |
| G-TAXI-06(境界値: radiusCV<=0.35) | 角を丸めた、ほぼ円に近い四角形(円らしさが高い形)を描く。 | radiusCV条件(円形でないこと)を満たさず不成立(TREATとして誤認識されないかも確認)。 |
| G-TAXI-07(境界値: 犬から101px) | 犬の中心から101px離れた位置を中心に同様の四角形を描く。 | distance(centroid,dogCenter)超過で不成立。 |
| G-TAXI-08(識別性) | TAXI相当の四角形を、閉じずに(始点と終点を大きく離して)描く。 | closedRatio超過で不成立。 |

### 3.9 8種横断の識別性(誤認識防止)テスト

| ID | 操作手順 | 期待結果 |
|---|---|---|
| G-DISC-01 | TUG相当の高速往復ジグザグを、意図的に振幅を大きくしてSONGのwaveCount条件も満たしうる形にする(山谷3つ以上、かつ主軸幅90px以内、reversalsPrimary>=4、速度500px/s以上)。 | 5.2の優先順位(TUGはSONGより先に判定)により、TUGとして認識されることを確認する。 |
| G-DISC-02 | 大きめの四角(TAXI相当)と大きめの丸(HUG相当)の中間的な、角の丸い八角形を犬を囲むように描く(cornerCount・radiusCVが境界付近になるよう調整)。 | radiusCVの値によりTAXIかHUGのいずれか一方のみが成立し、両方成立・両方不成立(意図しない未認識)にならないことを複数回試行して確認する。 |
| G-DISC-03 | MULTI_PULL条件を満たさない2方位ストロークを3本連続で行った後、そのうち直近1本を単体で見たときにRETRACEやLEAD_TO_BAKERYの条件を偶然満たしてしまわないか、方向を変えて数パターン試す。 | 各ストロークが意図した単一ジェスチャー(またはMULTI_PULL不成立時は未認識)としてのみ扱われ、想定外のジェスチャーとして誤解除されないことを確認する。 |
| G-DISC-04 | LEAD_TO_BAKERYの正常系(G-BAKERY-01)を、lastHeadingがちょうどbakeryPos方向の反対(つまりretrace方向とbakery方向が一致するケース)になるよう犬を歩かせてから実施する。 | 5.2の優先順位でRETRACE(6)がLEAD_TO_BAKERY(7)より先に判定されるため、両条件を満たす場合はRETRACEとして認識されることを確認する(仕様どおりの優先順位動作の確認)。 |

---

## 4. ジェスチャー入力の異常系テスト

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| IN-01 | MIN_POINTS未満の入力無視 | REFUSING中、`#gestureCanvas`上でごく短時間のタップに近い操作(pointerdown直後にpointerup、点列が6点未満)を行う。 | 何のジェスチャーとしても判定されず、無視される(キャンバスがクリアされる程度で、誤ってジェスチャー成立扱いにならない)。拒否柴は継続する。 |
| IN-02 | MIN_DURATION未満の入力無視 | 6点以上の座標は記録されるが、pointerdown〜pointerupが80ms未満で完了する非常に素早い操作を行う(例: 短距離を瞬間的になぞる)。 | duration<80msのため「意味のある入力」とみなされず無視される。拒否柴は継続する。 |
| IN-03 | 未認識ストロークでのペナルティ無し | REFUSING中、8種いずれの条件も満たさない適当な軌跡(例: ランダムな殴り書き)を描く。 | 「ちがうよ」的な軽いリアクション(頭をかしげるアニメーション、約0.3秒)が再生され、キャンバスがクリアされる。スコアが減点されない(現在のepisodeScoreの計算対象時間が伸びる=結果的に不利にはなるが、即時のペナルティ処理は発生しない)ことを確認する。 |
| IN-04 | 未認識後の再挑戦が可能であること | IN-03の直後、続けて正しいジェスチャー(例: TUG)を描く。 | 直前の失敗が尾を引かず、正しいジェスチャーとして問題なく認識・解除される。試行回数に制限がない。 |
| IN-05 | 未認識を複数回繰り返した場合の継続動作 | REFUSING中に未認識ストロークを5回以上連続して描く。 | 毎回同様に「ちがうよ」リアクション+キャンバスクリアが行われ、拒否柴状態・タイマー(`#refusalBanner`のカウントアップ)は継続して進み続ける。エラーやフリーズが発生しない。 |
| IN-06 | WALKING中はジェスチャーが無効(pointer-events) | WALKING中に`#gestureCanvas`上でTUG等の正しい軌跡をドラッグしてみる。 | WALKING中は`#gestureCanvas`が`pointer-events:none`のため、ドラッグ操作が犬の移動やゲーム状態に一切影響しない(誤ってジェスチャー判定が走らない)。 |
| IN-07 | ストローク点数ちょうど境界(6点) | 開発者ツールで座標点を6点ちょうど生成するよう操作(または合成イベント)し、他は正常なTUG条件を満たす形にする。 | MIN_POINTS=6を満たすため、他条件が揃っていれば正しくTUGとして認識される(5点以下は不成立、6点は成立、という境界の確認)。 |
| IN-08 | duration境界(80ms) | 合成イベントでduration=79msと81msの2パターンを用意し、他条件は同一にする。 | 79msは無視、81msは(他条件を満たせば)ジェスチャーとして判定処理の対象になることを確認する。 |

---

## 5. スコア計算のテスト

計算式: `episodeScore = round(clamp(100 - 6*elapsedSeconds, 20, 100))`

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| SC-01 | 即時解除(0秒付近)の高得点 | 拒否柴発生直後、できるだけ速やかに(1秒未満で)正しいジェスチャーを成立させる。 | 獲得スコアが100点に近い値(理論値: 1秒なら100-6=94点程度)になる。スコアポップアップに表示される値と一致することを確認する。 |
| SC-02 | 中間時間での減衰 | 拒否柴発生から約5秒後にジェスチャーを成立させる。 | 理論値 `100-6*5=70点` 付近のスコアが加算される。 |
| SC-03 | 下限到達の境界(13.3秒) | 拒否柴発生から約13.3秒後(`(100-20)/6`)にジェスチャーを成立させる。 | スコアが下限20点付近になる。 |
| SC-04 | 下限到達後の頭打ち確認 | 拒否柴発生から20秒以上経過させてからジェスチャーを成立させる(未認識ストロークを繰り返して時間を稼いでもよい)。 | スコアが20点を下回らず、20点で頭打ちになる(MIN_SCOREでclampされている)。 |
| SC-05 | 合計スコアの加算(拒否柴分) | 複数エピソード(2〜3回)を異なる解除時間で完了させ、都度のepisodeScoreを記録する。 | `#scoreValue`(合計スコア)が各episodeScoreの累積と一致する(花ボーナスが無い場合)。 |
| SC-06 | 花ボーナスとの合算 | WALKING中に花に1〜2個触れてflowerBonusTotalを加算させたうえで、拒否柴を1回解除する。 | 合計スコアが `Σepisode Score + flowerBonusTotal` と一致する。RESULT画面でも花ボーナス合計欄と拒否柴分の内訳が分けて表示され、合算値が合計スコアと一致する(6.3節)。 |
| SC-07 | スコアポップアップの表示値一致 | 任意のタイミングでジェスチャーを成立させる。 | 犬の位置に表示される`+xx`ポップアップの数値が、実際に加算されたepisodeScoreと一致する。 |
| SC-08 | 未認識ストロークによる即時減点が無いことの確認 | REFUSING中に未認識ストロークを描いた直後の合計スコアを確認する。 | 未認識ストローク単体ではスコアが変化しない(減点されない)。ただし経過時間が伸びるため、最終的なepisodeScoreは相対的に下がる(SC-02等と比較して間接的に確認)。 |

---

## 6. UI/表示のテスト

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| UI-01 | `#refusalBanner`のカウントアップ表示 | REFUSING突入直後から数秒間、バナーの数値を継続的に観察する。 | 「拒否柴発生！」等の文言とともに、経過時間が0.1秒刻みで増加表示される(例: `0.1秒`→`0.2秒`→…→`3.2秒`)。表示がフリーズ・逆行しない。 |
| UI-02 | `#refusalBanner`の表示/非表示切り替え | REFUSING突入時とジェスチャー成立(解除)時それぞれを観察する。 | REFUSING中のみ表示され、解除された瞬間に非表示になる。 |
| UI-03 | `#gestureCanvas`の軌跡描画 | REFUSING中にドラッグ操作を行い、指(ポインタ)の動きを目視する。 | ドラッグした軌跡がポリライン(手描き風、太さ3〜4px、暖色、やや半透明)としてリアルタイムに描画される。pointerupまたは次のpointerdownで軌跡がクリアされる。 |
| UI-04 | `#gestureCanvas`のpointer-events切り替え | WALKING中とREFUSING中それぞれで、canvas領域をクリック/ドラッグしてみる(開発者ツールでCSSの`pointer-events`値も確認)。 | WALKING中は`pointer-events:none`(ドラッグしても軌跡が描かれない/下の要素に干渉しない)、REFUSING中は`pointer-events:auto`(軌跡が描ける)。 |
| UI-05 | `#bakeryIcon`の常時表示 | BREED_SELECT後のWALKING中、REFUSING中それぞれの画面を確認する。 | いずれの状態でも画面内の固定位置にパン屋アイコン(絵文字🥖等+看板風装飾)が表示され続けている。位置がゲーム中に動かない。 |
| UI-06 | `#episodeCounter`の進捗表示 | 拒否柴を1回解除するごとにカウンタ表示を確認する。 | 「拒否柴 n / 8」のような表示で、nが解除するたびに1ずつ増加する。初期値は0または1(実装依存だが、8回解決した時点で「8/8」相当になっていること)。 |
| UI-07 | RESULT画面の内訳表示 | 8回(または一部)エピソードを消化してRESULT画面に到達させる。 | 合計スコアが大きく表示され、各エピソードの`#, かかった時間(秒), 獲得スコア`の内訳テーブルが表示される。行数がエピソード消化数と一致し、各行の時間・スコアが実際のプレイ内容と整合する。 |
| UI-08 | RESULT画面の花ボーナス表示 | 花を数個収集した状態でRESULT画面に到達させる。 | 花ボーナス合計が個別に表示され、合計スコアに正しく反映されている。 |
| UI-09 | RESULT画面の再プレイボタン | RESULT画面で「もう一度あそぶ」ボタンの見た目・押下反応を確認する。 | ボタンが視認・クリック可能で、押下するとBREED_SELECTへ遷移する(ST-07と重複確認)。 |
| UI-10 | 拒否柴突入時の吹き出し演出 | REFUSING突入の瞬間を観察する。 | 犬の頭上に吹き出し(💭や🚫等)がふわっと表示されるアニメーションが確認できる。 |
| UI-11 | 拒否柴解除時の成功エフェクト | ジェスチャー成立の瞬間を観察する。 | 犬の周囲に星やハート等が散る短い成功エフェクトアニメーションが再生される。 |

---

## 7. ビジュアル刷新の確認項目(目視チェックリスト、7章対応)

各項目は犬種選択画面のプレビューおよびゲーム中の犬(3配色: 茶柴/白柴/黒柴)の両方で確認する。

| ID | 確認項目 | 確認手順 | 合格基準 |
|---|---|---|---|
| VIS-01 | 輪郭の非対称化 | body・headの形状を目視する。 | 完全な円/楕円ではなく、`border-radius`の非対称指定により有機的でいびつな輪郭になっている(現行の単純な円形から明確に変化している)。 |
| VIS-02 | 毛並みのグラデーション | body等の配色を目視する。 | 単色ベタ塗りではなく、明→中間→暗の多段グラデーション(`radial-gradient`)による陰影・立体感が確認できる。 |
| VIS-03 | ほっぺの毛 | muzzle周辺を目視する。 | 左右に丸いふくらみ(柴犬特有のまるいほっぺ)が追加されている。 |
| VIS-04 | 立ち耳 | earの形状を目視する。 | 単純な丸みではなく、三角形寄りの形状(`clip-path`ベース)になっており、内耳が別色で表現されている。 |
| VIS-05 | 巻き尾 | tailの形状を目視する。 | 左右非対称な`border-radius`で「く」の字/巻き尾らしいシルエットになっており、先端に色の変化(毛先感)がある。 |
| VIS-06 | 目の表現 | eyeを拡大目視する(必要ならブラウザのズーム機能を使用)。 | 非対称形状+内側ハイライト(`box-shadow: inset`)が確認できる。茶柴・黒柴では眉(タン模様)らしき小さな楕円が追加されている。 |
| VIS-07 | 接地感(影) | 犬の足元を目視する。 | 半透明の楕円影が足元に表示され、犬全体にもドロップシャドウがかかっている。 |
| VIS-08 | アイドルアニメーション | WALKING中、矢印キーを押さず静止させた状態で数秒観察する。 | 呼吸のような緩やかな`scaleY`アニメーションが確認でき、完全な静止画になっていない。 |
| VIS-09 | 歩行時の尻尾アニメーション | 矢印キーで移動させながら尻尾を観察する。 | 移動中、尻尾が左右に揺れる(`rotate`往復)アニメーションが確認できる。 |
| VIS-10 | 拒否柴ポーズの整合性 | REFUSING突入時の見た目全体を確認する。 | 新ビジュアル基盤上でも脚が隠れる・体が沈む・頭が傾く等の座り込み変形が違和感なく反映されている(輪郭が崩れたり要素が変な位置にはみ出したりしない)。 |
| VIS-11 | 配色の踏襲 | 茶柴/白柴/黒柴それぞれを確認する。 | グラデーション化後も各breedの主色(茶柴`#cf8a3d`系、白柴`#fbf6ea`系、黒柴`#3b332c`系)を基調にしており、3犬種の見分けが引き続き容易である。 |
| VIS-12 | 犬種選択画面プレビューへの反映 | BREED_SELECT画面の`.dog-choice .preview`を確認する。 | ゲーム中の犬と同じ強化済みスタイルが選択画面のプレビューにも反映されている。 |

---

## 8. 終了条件のテスト

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| END-01 | 8エピソード到達での終了 | 拒否柴イベントを1〜8回目まで順に解決していく(各回、任意のジェスチャーで解除)。 | 8回目の解除が完了した直後、9回目の拒否柴が発生することなく`RESULT`画面に遷移する。`#episodeCounter`が最終的に「8/8」相当を示す。 |
| END-02 | 7回終了時点では終了しないこと | 7回のみ解決し、その後の挙動を確認する。 | RESULTに遷移せず、通常どおり次(8回目)の拒否柴発生を待つ状態が継続する。 |
| END-03 | 5分セーフティタイムアウト(強制終了) | ゲーム開始(BREED_SELECT選択)から5分(300,000ms)経過するまでプレイを続ける(エピソード消化数が8未満のまま経過させる)。実施が困難な場合は、開発者ツールで`performance.now`/`Date.now`をモックする、あるいはデバッグ用に`SAFETY_TIME_LIMIT_MS`を一時的に短縮したビルドで代替検証してもよい(本番相当の実装では実測での5分待機を最低1回は実施することを推奨)。 | 経過時間が5分を超えた時点で、エピソード消化数が8未満であっても強制的に`RESULT`画面に遷移する。 |
| END-04 | タイムアウトとエピソード完了が競合するケース | 7回目解決直後、8回目の拒否柴発生前後で5分の閾値を跨ぐタイミングを作る(またはデバッグでタイマーを操作)。 | どちらの条件が先に成立してもゲームがフリーズ・二重遷移せず、正しく1回だけRESULT画面に遷移する。 |
| END-05 | RESULT画面到達後にゲームロジックが停止すること | RESULT画面到達後、矢印キーやgestureCanvasへの操作を試みる。 | RESULT表示中は犬の移動・拒否柴タイマー・スコア加算が一切発生しない(バックグラウンドで進行し続けない)。 |

---

## 9. リグレッションテスト(既存の花コレクション機能)

| ID | テスト対象 | 操作手順 | 期待結果 |
|---|---|---|---|
| REG-01 | マーキング生成(WALKING中) | WALKING中、矢印キーで犬を42px以上連続して移動させる。 | 移動した跡にマーキング(`.marking`)が生成される(現行仕様どおり、約42px移動ごとに1つ)。 |
| REG-02 | マーキング→花への変化 | マーキング生成後、3〜6秒待機する。 | マーキングが消え、代わりに花(ひまわり/チューリップ/ミモザのいずれか)が同じ位置に出現する。 |
| REG-03 | 花への接触でスコア加算 | 犬を花の位置まで移動させて接触させる。 | 花が消え、花の種類に応じたスコア(ひまわり50/チューリップ10/ミモザ5)が加算され、スコアポップアップが表示される。 |
| REG-04 | REFUSING中はマーキングが生成されないこと | 拒否柴発生直前まで移動して停止(座り込み)させた後、REFUSING中に矢印キーを押し続ける。 | 犬が動かないため新規マーキングが生成されない(ST-03と関連)。 |
| REG-05 | REFUSING中に既存の花への接触判定が停止していること | REFUSING突入時点で花が近くに残っている状態を作る。 | 犬が静止しているため、REFUSING中に自動でスコアが増えることはない(接触判定自体は犬の移動に付随するため、移動しない限り発生しない)。 |
| REG-06 | 花ボーナスの独立集計 | 花を複数個(異なる種類)収集し、拒否柴も複数回解決する。 | `flowerBonusTotal`(花由来のスコア)と拒否柴由来のepisodeScore合計が区別して管理され、RESULT画面でそれぞれ確認できる(SC-06と関連)。 |
| REG-07 | 花の種類・重み・得点が変更されていないこと | 十分な回数花を収集し、出現比率・得点を観察する(統計的な確認)。 | ひまわり(50点, 出現重み15)・チューリップ(10点, 重み35)・ミモザ(5点, 重み50)のまま変更されていない(既存コードの`FLOWER_TYPES`定義を踏襲)。 |
| REG-08 | 犬種選択・移動・境界制限の基本動作 | 3犬種それぞれを選択し、矢印キーで画面端まで移動させる。 | 現行同様、犬が画面外にはみ出さず、境界でクランプされる。速度(220px/s相当)も現行から変化していない。 |
| REG-09 | 単一HTMLファイル・外部依存なしでの動作 | `index.html`単体をブラウザで直接開く(ローカルファイル、サーバー不要)。開発者ツールのコンソールでエラーの有無を確認する。 | 外部ライブラリ・ビルドツールへの依存なしに動作し、JSエラーが出力されない(11章の受け入れ基準最終項目)。 |

---

## 付録: 受け入れ基準(spec.md 11章)とのトレーサビリティ

| spec.md 11章の受け入れ基準項目 | 対応する主なテストID |
|---|---|
| 犬種選択〜見た目反映 | ST-01, VIS-11, VIS-12, REG-08 |
| WALKING中の矢印キー移動 | ST-01, REG-08 |
| マーキング→花変化 | REG-01, REG-02 |
| 花接触でスコア加算 | REG-03, SC-06 |
| 初回1.2秒+30px、以降4〜9秒発生 | TR-01〜TR-09 |
| 拒否柴ポーズ・`#refusalBanner`カウントアップ | ST-02, UI-01, UI-02, VIS-10 |
| REFUSING中の入力無効・マーキング停止 | ST-03, REG-04, REG-05 |
| `#gestureCanvas`の軌跡描画 | UI-03, UI-04 |
| 8種ジェスチャーの独立成立 | G-TUG-*, G-MULTI-*, G-RETRACE-*, G-BAKERY-*, G-TREAT-*, G-SONG-*, G-HUG-*, G-TAXI-* |
| 認識成功で解除・WALKING復帰 | ST-04 |
| 認識失敗時のペナルティ無し・再挑戦可 | IN-03, IN-04, IN-05 |
| 経過時間ベースのスコア(20〜100点) | SC-01〜SC-04 |
| 8回解決または5分でRESULTへ | END-01, END-03 |
| RESULT画面の内訳・再プレイでリセット | UI-07, UI-08, UI-09, ST-07 |
| `#bakeryIcon`常時表示 | UI-05 |
| ビジュアル刷新(非対称輪郭等) | VIS-01〜VIS-10 |
| 単一HTML・外部依存なし・JSエラー無し | REG-09 |
