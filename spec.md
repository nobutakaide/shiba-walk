# 柴犬のお散歩ゲーム リニューアル仕様書 「拒否柴解放ゲーム」

version: 0.1 draft
対象ファイル: `index.html`(単一ファイル構成を維持。外部ライブラリ・ビルド不要、vanilla HTML/CSS/JSのみ。SVGはインラインで使用可)

---

## 1. 背景・目的

現行の「柴犬のお散歩ゲーム」は、犬種を選んで矢印キーで散歩させ、通った道に残るマーキングが花に変わり、花に触れるとスコアが増えるという牧歌的な収集ゲームである。実装はCSSのdiv図形合成による柴犬(body/head/ear/muzzle/nose/eye/tail/legの11要素)、ゲームループ(`requestAnimationFrame`)、マーキング→花への時限変化、円形当たり判定によるスコアという最小限のメカニクスで構成されている。

本リニューアルでは、この基盤を活かしつつ以下の2点を軸に大幅刷新する。

1. **ビジュアル刷新**: 柴犬の見た目を、手描き風の質感・柴犬らしい輪郭(丸いほっぺ、立ち耳、巻き尾)を持つイラストタッチへ改善する。
2. **コンセプト変更**: メインメカニクスを「花コレクション」から「**拒否柴解放**」へ転換する。散歩中ランダムに犬が座り込んで動かなくなる「拒否柴状態」が発生し、プレイヤーはマウス/ペン(ポインタードラッグ)でジェスチャーを描いて機嫌を直し、解除までのタイムを競う。

花コレクション要素は完全に廃止はせず、背景演出兼サブスコアとして残す(詳細は8章)。

---

## 2. 用語定義

| 用語 | 意味 |
|---|---|
| 拒否柴(きょひしば) | 柴犬が突然座り込み、動かなくなる状態。俗に「散歩を拒否する柴犬」を指すネットスラング。本仕様のメイン障害イベント。 |
| 拒否柴イベント/エピソード | 1回の「拒否柴状態の発生→解除」のサイクル。 |
| ジェスチャー | プレイヤーがポインタードラッグで描く、拒否柴解除のためのアクション(8種類、4章参照)。 |
| ストローク | pointerdown〜pointerupの1回分のドラッグ軌跡(座標点列)。 |
| ラウンド | ゲーム開始からリザルト画面までの1プレイ。既定で拒否柴イベント8回で終了。 |

---

## 3. ゲーム全体のフロー

```
[犬種選択画面 BREED_SELECT]
        │ 犬種クリック
        ▼
[散歩中 WALKING] ──(乱数タイマー発火)──▶ [拒否柴中 REFUSING]
        ▲                                        │
        │                 ジェスチャー成功で解除    │
        └────────────────────────────────────────┘
        │
        │ 拒否柴イベントを既定回数(8回)解決 or 総プレイ時間が上限(5分)到達
        ▼
[リザルト画面 RESULT] ──「もう一度あそぶ」──▶ [犬種選択画面 BREED_SELECT]
```

### 3.1 状態(ステート)一覧

- `BREED_SELECT`: 現行の犬種選択UIをそのまま使用(茶柴/白柴/黒柴)。選択後 `WALKING` へ。
- `WALKING`: 矢印キーで移動可能。マーキング/花の生成・収集は現行仕様を継続。乱数タイマーにより `REFUSING` へ遷移する。
- `REFUSING`: 矢印キー入力は無視(移動しない)。ポインタードラッグ(ジェスチャー)の受付を開始。いずれかのジェスチャーが認識されると `WALKING` に復帰し、経過時間に応じてスコア加算。
- `RESULT`: 拒否柴イベントを既定回数解決、または総経過時間が上限に達した時点で遷移。合計スコア・各エピソードの内訳を表示し、再プレイ導線を出す。

### 3.2 セーフティ/終了条件

- 既定終了条件: 拒否柴イベントを **8回**(定数 `TOTAL_EPISODES = 8`)解決した時点で `RESULT` へ。
- フェイルセーフ: ゲーム開始からの総経過時間が **5分(300,000ms)** を超えた場合、エピソード回数に関わらず強制的に `RESULT` へ遷移する(プレイヤーが詰まった場合の保険)。

---

## 4. 拒否柴状態の仕様

### 4.1 発生条件・頻度

- `WALKING` 状態に入る(ゲーム開始時、または前回の拒否柴解除時)たびに、次の拒否柴発生までのタイマーを **4,000〜9,000msのランダム値**(`nextRefusalDelay = 4000 + Math.random() * 5000`)でセットする。
- ただし初回発生は、犬が累計 **30px以上移動**してから、かつゲーム開始から最低 **1,200ms** 経過してから行う(オンボーディング用の猶予。プレイヤーが操作方法を確認できるようにする)。
- タイマー満了時点で `WALKING` → `REFUSING` に遷移する。移動距離条件は初回のみ。2回目以降は時間経過のみで発火してよい。

### 4.2 見た目変化(REFUSING突入時)

犬のDOM要素に `.refusing` クラスを付与し、以下のスタイル変化を行う。

- 犬全体のコンテナに `transform: translateY(6px) scaleY(0.8)` 相当を適用し、体高を下げて「座った」印象を出す。
- `.leg` を非表示または高さを1/3程度に短縮(`height`を縮める、または`display:none`にして接地感を出す)。
- `.head` に `rotate(-6deg)` 程度の傾きを与え、うつむき加減にする。
- `.ear` をやや外側・後方へ倒す角度に変更(不機嫌さの表現)。
- `.eye` を半目風(`height`を縮小 or `border-radius`変更)にする。
- 犬コンテナに `translateX` を ±2px, 周期0.4秒程度で往復させる微振動アニメーション(頑固さの表現、`@keyframes stubbornShake`)を付与する。
- 犬の頭上に吹き出し要素(例: 💭 or 🚫 の絵文字、または簡易雲形div)をCSSアニメーションでふわっと表示する。
- `#refusalBanner` をゲーム画面上部(scoreBar下)に表示: 「拒否柴発生！」の文言 + 経過時間を**カウントアップ表示**(0.1秒刻み、例: `3.2秒`)。この経過時間表示が視覚的な緊張感を生む。

解除時(`REFUSING` → `WALKING`):

- `.refusing` クラスを除去し、通常ポーズへ0.2秒程度でアニメーション復帰。
- 犬の周囲に小さな成功エフェクト(星やハートが散る、`scoreClearBurst`的な短いCSSアニメーション)を表示。
- `#refusalBanner` を非表示にし、獲得スコアのポップアップ(既存 `.scorePopup` の仕組みを流用)を犬の位置に表示。

### 4.3 REFUSING中の入力仕様

- `keydown`/`keyup` による矢印キー移動処理は `state === "REFUSING"` の間スキップする(dx, dyを計算しない、または計算しても移動適用しない)。
- マーキング生成(`addMarking`)も `REFUSING` 中は行わない(犬が動いていないため)。
- `#gestureCanvas`(4.4節)がポインターイベントを受け付ける。

### 4.4 ジェスチャー入力の受付とストローク管理

- `#gameArea` の上に透明な `<canvas id="gestureCanvas">` を重ねる(`position:absolute; inset:0; z-index: 20`)。`REFUSING` 状態のときのみ `pointer-events: auto`、`WALKING` 中は `pointer-events: none`。
- `pointerdown`: 新しいストローク配列 `currentStroke = []` を開始し、キャンバスをクリア。
- `pointermove`(ボタン押下中のみ): `{x, y, t: performance.now()}` を `currentStroke` に追加し、直前点からポリラインを描画(フェードする手描き風の軌跡線。太さ3〜4px、犬の毛色に合わせた暖色、少し透明度を持たせる)。
- `pointerup`: ストローク確定 → 4.5節の判定ロジックを実行。
  - 認識成功 → 拒否柴解除処理(4.2節)を実行し、経過時間からスコアを計算(5章)。
  - 認識失敗(どのジェスチャーにも該当しない) → キャンバスをクリアし、犬に小さな「ちがうよ」リアクション(頭をかしげるアニメーション、0.3秒)を再生。タイマーは継続。プレイヤーは何度でも再挑戦できる(失敗ペナルティなし。ただし時間経過そのものがスコア減点要因になる)。
- ストローク点数が `MIN_POINTS = 6` 未満、または `duration < MIN_DURATION(80ms)` の場合は「意味のある入力」とみなさず無視する(誤タップ対策)。

---

## 5. ジェスチャー判定ロジック(8種)

### 5.1 共通の前処理・特徴量

ストローク `points = [{x,y,t}, ...]` から以下を算出する。

| 特徴量 | 定義 |
|---|---|
| `pathLength` | 連続点間距離の総和 |
| `displacement` | `distance(points[0], points[last])` |
| `duration` | `t_last - t_first` (ms) |
| `bboxW, bboxH` | 軌跡のバウンディングボックス幅・高さ |
| `centroid` | 全点の平均座標 |
| `straightness` | `displacement / pathLength`(1に近いほど直線的) |
| `closedRatio` | `displacement / max(bboxW, bboxH, 1)`(小さいほど始点と終点が近い=閉じたループ) |
| `avgRadius` | `centroid`から各点までの距離の平均 |
| `radiusCV` | `avgRadius`の標準偏差 ÷ `avgRadius`(円らしさ。小さいほど正円に近い) |
| `reversalsPrimary` | 主軸(bboxWとbboxHの大きい方の軸)上で、隣接点間の移動方向(符号)が反転した回数。ジッター対策として1ステップの移動量が2px未満の場合はカウントしない |
| `waveCount` | 副軸(主軸と直交する軸)方向の極大・極小点の数(移動量8px以上を有効な山/谷とみなす) |
| `cornerCount` | 点列を5点おきにサンプリングし、直前セグメントと直後セグメントのなす角が60°〜120°の範囲にある「角」の数 |
| `avgSpeed` | `pathLength / (duration/1000)` (px/秒) |
| `startDistToDog` | `distance(points[0], dogCenter)` |
| `enclosesDog` | `closedRatio <= 0.35` かつ 犬のバウンディングボックス4隅がすべて `centroid` から `[avgRadius*0.6, avgRadius*1.4]` の範囲内にある |

`dogCenter` は現在の犬の中心座標、`bakeryPos` はパン屋アイコンの固定表示座標(6.4節)。`lastHeading` は拒否柴発生直前1.5秒間の犬の正味移動方向(ラジアン)。犬がまだ一度も移動していない状態で最初の拒否柴が発生することはない(4.1節の初回条件により回避済み)。

共通の前提条件(定数):

```
MIN_POINTS = 6
MIN_DURATION = 80  // ms
DOG_PROXIMITY_RADIUS = 70 // px
```

### 5.2 判定優先順位

1回のストロークは以下の順で判定し、**最初に条件を満たしたジェスチャーを採用**する(1ストローク=1ジェスチャー種別のみ確定)。ただし②の「色々な方向へ引っ張る」は複数ストロークにまたがる判定のため、ストローク確定のたびに①として最優先でチェックする。

```
0. 複数方向プル判定用バッファを更新(直近3秒のストローク履歴に今回のストロークを追加)
1. MULTI_PULL(複数方向へ引っ張る) を判定 → 満たせば確定・バッファをクリア
2. TAXI(タクシーを呼ぶ)
3. TREAT(おやつをあげる)
4. HUG(抱っこする)
5. TUG(ぐいぐい引っ張る)
6. RETRACE(来た道を戻る)
7. LEAD_TO_BAKERY(パン屋に連れていく)
8. SONG(歌をうたう)
9. どれにも該当しなければ「未認識」(4.4節の失敗処理)
```

### 5.3 各ジェスチャーの条件

**① ぐいぐい引っ張る(TUG)**
- `startDistToDog <= DOG_PROXIMITY_RADIUS`
- `duration <= 1200ms`
- `reversalsPrimary >= 4`
- `avgSpeed >= 500 px/s`
- 主軸方向のbbox幅 15〜90px、副軸方向のbbox幅 < 40px(小刻みな往復)

**② 色々な方向へ引っ張ってみる(MULTI_PULL)**
- 直近3秒間のローリングバッファ内に、以下を満たすストロークが**3本以上**存在する:
  - `straightness >= 0.7`
  - `pathLength >= 40px`
  - `startDistToDog <= DOG_PROXIMITY_RADIUS`
- それら3本以上のストロークの方向角(`atan2(dy,dx)`)を45°刻み8方位(オクタント)に分類したとき、**異なる方位が3つ以上**含まれる。

**③ 来た道を戻ってあげる(RETRACE)**
- `straightness >= 0.75`
- `startDistToDog <= DOG_PROXIMITY_RADIUS`
- `pathLength >= 60px`
- ストローク方向角が `lastHeading + 180°` の ±30° 以内(逆方向へのドラッグ)

**④ 近くのパン屋に連れていく(LEAD_TO_BAKERY)**
- `startDistToDog <= DOG_PROXIMITY_RADIUS`
- `distance(points[last], bakeryPos) <= 40px`(終点がパン屋アイコン付近)
- `straightness >= 0.6`
- ストローク方向角が `angle(dogCenter → bakeryPos)` の ±25° 以内
- `pathLength >= distance(dogCenter, bakeryPos) * 0.6`(十分な距離を実際にドラッグしている)

**⑤ おやつをあげる(TREAT・小さい丸)**
- `closedRatio <= 0.35`
- `avgRadius` が 8〜30px
- `radiusCV <= 0.35`
- `distance(centroid, dogCenter) <= DOG_PROXIMITY_RADIUS`

**⑥ 歌をうたう(SONG・波線/音符風)**
- `closedRatio > 0.5`(閉じていない)
- `waveCount >= 3`
- `duration` が 400〜3000ms
- `startDistToDog <= 90px`

**⑦ 抱っこする(HUG・犬を囲む大きい丸)**
- `enclosesDog === true`
- `avgRadius` が 35〜100px
- `radiusCV <= 0.35`
- `distance(centroid, dogCenter) <= 25px`

**⑧ タクシーを呼ぶ(TAXI・四角形)**
- `closedRatio <= 0.35`
- `cornerCount >= 3`
- `bboxW / bboxH` が 0.4〜2.5(縦横比が極端でない)
- `radiusCV > 0.35`(円形ではないことの確認)
- `distance(centroid, dogCenter) <= 100px`

各しきい値は実装時の調整を許容する参考値であり、体感難易度が高すぎ/低すぎる場合はチューニングしてよい。ただし「8種類それぞれが独立に成立し得ること」「1ストロークまたは短時間の複数ストロークで無理なく描けること」を満たすこと。

---

## 6. スコア計算

### 6.1 拒否柴解除スコア

```
BASE_SCORE = 100
DECAY_PER_SEC = 6
MIN_SCORE = 20

elapsedSeconds = (解除時刻 - 発生時刻) / 1000
episodeScore = round(clamp(BASE_SCORE - DECAY_PER_SEC * elapsedSeconds, MIN_SCORE, BASE_SCORE))
```

- 0秒に近いほど100点、約13.3秒(`(100-20)/6`)以降は下限の20点で頭打ちになる。
- 認識失敗(未認識ストローク)はスコア減点しないが、時間経過そのものが `episodeScore` を下げるため実質的なペナルティとなる。
- 1エピソードにつき `episodeScore` を加算し、加算時に犬の位置へ `+xx` のポップアップ(既存 `.scorePopup` 流用)を表示する。

### 6.2 合計スコア

```
totalScore = Σ(episodeScore, 全8エピソード) + flowerBonusTotal
```

`flowerBonusTotal` は8章の花コレクション由来の加点(既存の5/10/50点をそのまま踏襲)。

### 6.3 リザルト画面の表示内容

- 合計スコア(大きく表示)
- 各エピソードの内訳テーブル: `#, かかった時間(秒), 獲得スコア`(使用ジェスチャー種別の表示は任意。実装が容易なら表示してもよいが必須ではない)
- 花ボーナス合計
- 「もう一度あそぶ」ボタン → `BREED_SELECT` へ状態リセット(スコア・エピソード数・花・マーキングを全てクリア)

---

## 7. ビジュアル刷新の方向性

既存のDOM構造(`body/head/ear.left/ear.right/muzzle/nose/eye.left/eye.right/tail/leg.l1/leg.l2`)は**維持**し、CSSの装飾を強化する形でリニューアルする(要素の全面SVG置き換えは必須ではないが、実装が容易であれば装飾要素として部分的にインラインSVGを併用してよい)。

具体的な改善方針:

1. **輪郭の非対称化**: `border-radius` に4値/8値指定(例: `58% 42% 55% 45% / 45% 48% 52% 55%`)を用い、body・headを完全な円/楕円ではない有機的な形状にする。
2. **毛並みのグラデーション**: 各breedの単色 `background` を、`radial-gradient` による多段階の陰影(明→中間色→暗)に置き換え、立体感を出す。
3. **ほっぺの毛(柴犬らしさの核)**: `.muzzle` の左右に `::before`/`::after` で丸いふくらみ(`radial-gradient`の円)を追加し、柴犬特有の「まるいほっぺ」を表現する。
4. **立ち耳**: `.ear` を `border-radius` ベースの丸みではなく `clip-path: polygon(...)` で三角形寄りの形状にし、内耳を `::after` の別色で表現する。
5. **巻き尾**: `.tail` の `border-radius` を非対称(例: `60% 40% 60% 40%`)にして「く」の字/巻き尾らしいシルエットにし、先端に薄い色のグラデーションで毛先感を出す。
6. **目**: `border-radius`非対称化 + `box-shadow: inset` で小さなハイライト(生き生きとした目)。breedごとに「柴犬眉(タン模様の眉毛部分)」を `::before` の小さな楕円で追加(特に茶柴・黒柴)。
7. **接地感**: 犬の足元に半透明の楕円影 `.dog .shadow`(`filter: blur()`または`radial-gradient`)を追加し、`filter: drop-shadow(...)` を犬全体にも軽くかける。
8. **アイドル/歩行アニメーション**: 常時ゆるやかな呼吸アニメーション(`scaleY`を2秒周期で微振動)、歩行中は尻尾の揺れアニメーション(`rotate`往復)を追加し静止画感を減らす。
9. **拒否柴ポーズ**: 4.2節の座り込み変形をこの新しいビジュアル基盤の上に適用する(既存より曲線が増えるため、脚の隠し方・体の潰し方も違和感なく繋がるよう調整する)。
10. **配色**: 既存の3配色(茶柴 `#cf8a3d`系、白柴 `#fbf6ea`系、黒柴 `#3b332c`系)を基調としつつ、単色→グラデーション化した際の主色として維持する(breedごとの見分けやすさを損なわない)。

犬種選択画面のプレビュー(`.dog-choice .preview`)も同じ強化済みスタイルがそのまま反映される(共通クラスを使っているため追加対応は基本不要)。

---

## 8. 既存の花コレクション要素の扱い

- **廃止しない。ただしサブ要素に格下げする。**
- `WALKING` 状態中のみ、現行仕様どおりマーキング生成(42px移動ごと)→3〜6秒後に花へ変化→接触でスコア加算、を継続する。
- `REFUSING` 状態中はマーキング生成を止める(犬が動かないため自然に発生しない。既存コードの移動処理をスキップすれば副作用として実現される)。
- 花由来のスコアは `flowerBonusTotal` として拒否柴スコアとは区別して管理し、リザルト画面で内訳として表示する(6.3節)。合算した `totalScore` を最終スコアとする。
- 花の種類・重み・得点(ひまわり50/チューリップ10/ミモザ5)は現行のまま変更不要。

---

## 9. 追加UI要素

| 要素ID | 内容 | 表示タイミング |
|---|---|---|
| `#refusalBanner` | 「拒否柴発生！」+ 経過秒数カウントアップ表示 | `REFUSING` 中のみ |
| `#gestureCanvas` | ポインタードラッグの軌跡を描画する透明canvas、`#gameArea`に重ねる | 常時DOM存在。ポインター受付は`REFUSING`中のみ有効 |
| `#bakeryIcon` | パン屋アイコン(絵文字🥖等 + 簡易看板風div)。画面内の固定位置(例: ゲームエリア右下から一定オフセット)に常時表示 | `WALKING`/`REFUSING`共通で常時表示 |
| `#episodeCounter` | 「拒否柴 n / 8」のような進捗表示。`#scoreBar`内に追加 | 常時表示(プレイ中) |
| `#resultScreen` | リザルト画面(合計スコア、内訳テーブル、再プレイボタン) | `RESULT`状態のときのみ |

`#bakeryIcon` は当たり判定を持たない装飾兼ジェスチャーの目印であり、犬との衝突判定は不要。

---

## 10. 状態遷移・定数まとめ

```js
// エピソード/終了条件
TOTAL_EPISODES = 8
SAFETY_TIME_LIMIT_MS = 300000 // 5分

// 拒否柴発生タイミング
NEXT_REFUSAL_MIN_MS = 4000
NEXT_REFUSAL_MAX_MS = 9000
FIRST_REFUSAL_MIN_ELAPSED_MS = 1200
FIRST_REFUSAL_MIN_DISTANCE_PX = 30

// ジェスチャー判定共通
MIN_POINTS = 6
MIN_DURATION_MS = 80
DOG_PROXIMITY_RADIUS = 70

// スコア
BASE_SCORE = 100
DECAY_PER_SEC = 6
MIN_SCORE = 20
```

---

## 11. 受け入れ基準(完成の定義)

- [ ] 犬種選択画面(茶柴/白柴/黒柴)から開始でき、選んだ犬種の見た目がゲーム中も反映される
- [ ] `WALKING` 中、矢印キーで犬が上下左右に移動できる(現行の速度・境界制限を維持)
- [ ] 歩いた跡にマーキングが表示され、3〜6秒後に花(ひまわり/チューリップ/ミモザ)に変化する(現行仕様を維持)
- [ ] 花に触れると `flowerBonusTotal` に加点され、スコアポップアップ演出が出る
- [ ] ゲーム開始後、初回は最低1.2秒+30px移動を経てから、以降は4〜9秒のランダム間隔で犬が「拒否柴状態」になる
- [ ] 拒否柴状態になると犬が座り込みポーズ(脚が隠れる/体が沈む/頭が傾く等)に変化し、`#refusalBanner` に経過時間がカウントアップ表示される
- [ ] 拒否柴中は矢印キー入力を受け付けず、犬が移動・マーキング生成を行わない
- [ ] 拒否柴中、`#gestureCanvas` 上でのポインタードラッグの軌跡が視覚的に描画される
- [ ] 5.3節で定義した8種類のジェスチャー(ぐいぐい引っ張る/色々な方向へ引っ張る/来た道を戻る/パン屋に連れていく/おやつ/歌/抱っこ/タクシー)がそれぞれ独立に判定・成立し得る(コードレビューまたは手動テストで各1回以上成功を確認できる)
- [ ] 8種のうちいずれか1つが正しく認識されると拒否柴状態が解除され、犬が `WALKING` に復帰する
- [ ] 認識に失敗したストロークではペナルティなくキャンバスがクリアされ、再挑戦できる
- [ ] 拒否柴発生から解除までの経過時間に基づき、6.1節の計算式でスコアが加算される(速いほど高得点、20〜100点の範囲)
- [ ] 拒否柴イベントを8回解決、または総経過時間が5分に達すると `RESULT` 画面に遷移する
- [ ] `RESULT` 画面で合計スコア・各エピソードのタイム/獲得点・花ボーナス合計が確認でき、「もう一度あそぶ」で `BREED_SELECT` からやり直せる(全状態がリセットされる)
- [ ] `#bakeryIcon` が画面内の固定位置に常時表示されている
- [ ] 犬のビジュアルが7章の方針に沿って刷新されている(非対称輪郭・グラデーション陰影・ほっぺの丸み・巻き尾など、単純な単色図形から明確に改善されている)
- [ ] 単一の `index.html` のみで完結し、外部ライブラリ・ビルドツールへの依存がなく、ブラウザで直接開いてJSエラーなく動作する
